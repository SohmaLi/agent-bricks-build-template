# 🤖 ANV_MCP: AUTONOMOUS WORKFLOW (BRICKS 1.12.3)

> **Đã hợp nhất (2026-07-14)**: File này trước đây mô tả một pipeline riêng (`section_autofix_loop.py`, `validate_plan_g0.py`, `visual_critic.py`...) — cả 3 script này **chưa từng tồn tại** trong `todo/scripts/`, và 3 script còn lại nó dùng (`validate_json.py`, `validate_structure.py`, `capture_screenshot.py`) là bản nháp đã bị bản mới hơn thay thế (`validate_template_json.py`, `validate_section_structure.py`, `capture_screenshot_logged_in.py`) rồi xoá. Nội dung còn giá trị (quy tắc an toàn, luồng G1→G4) đã có đầy đủ và cập nhật hơn tại:
>
> **[`AGENTS.md`](AGENTS.md)** — mục 2 (Quy tắc vàng), mục 3 (Luồng thực thi), mục 5 (Công cụ hiện có).
